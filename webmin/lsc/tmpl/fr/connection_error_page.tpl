<h1>Le destinataire demandé n'est pas joignable.</h2>
<u>Vous pouvez vérifier que :</u>
<ul>
<li>Le destinataire est allumé et connecté au réseau</li>
<li>Le destinataire dispose d'un serveur SSH installé et demarré</li>
<li>Le nom du destinataire dans le LRS correspond à celui de la machine sous windows, ou à celui de l'enregistrement DNS de la machine</li>
</ul>

<h2>Information sur l'erreur :</h2>

<p>Adresse MAC : {MAC}</p>
<p>Adresse IP : {IP}</p>
<p>Nom du poste : {HOSTNAME}</p>
<p>Ping : {PING}</p>
<p>Connexion ssh : {TEST_SSH}</p>
<ul>
	<li>command : {SSH_COMMAND}</li>
	<li>valeur de retour : {SSH_RETURN_VAL}</li>
	<li>sortie standard et sortie d'erreur : "{SSH_OUTPUT}"</li>
</ul>
<p>Montage autofs : {TEST_AUTOFS}</p>
