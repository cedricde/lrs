#!/bin/sh

set -e

action=$1
version=$2

. /usr/share/debconf/confmodule

if
	[ -n "$version" ] &&
	dpkg --compare-versions "$version" lt 0.8.3-1 &&
	! grep -q dhcp /etc/network/interfaces
then
	db_input high pump/old_config || :
	db_go
fi
